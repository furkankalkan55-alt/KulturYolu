{
  "name": "kultur-yolu-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Kültür Yolu quiz PWA wrapped with Capacitor for Android APK.",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}